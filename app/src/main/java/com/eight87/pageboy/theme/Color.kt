package com.eight87.pageboy.theme

import androidx.compose.ui.graphics.Color

// Brand seeds used for the dark-mode color scheme. `expressiveLightColorScheme()`
// covers light mode by itself; dark mode pairs `darkColorScheme()` with these
// hand-picked anchors so the surface-tier ladder still resolves correctly.
//
// Pageboy's brand palette leans cooler and quieter than the music apps —
// reading is a focused, low-chrome activity, so we avoid the punchier purples
// the music apps use.
val Slate80 = Color(0xFFB6C7DA)
val SlateGrey80 = Color(0xFFC0C6D2)
val Teal80 = Color(0xFFA0CFCC)

val Slate40 = Color(0xFF425B73)
val SlateGrey40 = Color(0xFF5B6470)
val Teal40 = Color(0xFF3D6663)
